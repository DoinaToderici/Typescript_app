import { FunctionComponent, useState } from "react";

type FormProps = {};

// Le type d'un seter react ex: setSeter()
// setSeter:Dispatch<setStateAction<number>>

export const Counter: FunctionComponent<FormProps> = ({}) => {
  return (
    <div>
      <form action=""></form>
    </div>
  );
};
